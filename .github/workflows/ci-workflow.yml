name: On merge to master workflow.

on:
  push:
    branch:
      - main

jobs:
  prepare:
    runs-on: ubuntu-20.04

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Build and test.
        uses: ./.github/actions/buildAndTest